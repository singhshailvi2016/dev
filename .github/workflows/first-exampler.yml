name: My first workflow

on: push

jobs:
    first-job:
        runs-on: ubuntu-latest
        steps:
        - name: welcome-message
          run: echo "my first Github action"
        - name: list-files
          run: ls
        - name: read the file
          run: cat README.md
